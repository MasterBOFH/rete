# reTerm

Prebuilt **reTerm** binaries are published under [**Releases**](https://github.com/MasterBOFH/rete/releases).

Source code for this project is **not** public. This repository exists to distribute signed builds and checksums only.

## Install

1. Open the [latest release](https://github.com/MasterBOFH/rete/releases/latest).
2. Download the archive for your platform (e.g. `reTerm-vX.Y.Z-macos-arm64.tar.gz` or `…-linux-x86_64.tar.gz`).
3. Verify the matching `.sha256` file if you rely on integrity checks.
4. Extract and run the `reTerm` binary as documented in your usual setup.

## Reporting issues

If you accept feedback in another place (private tracker, email, etc.), add that here. Example:

- *Issue tracking for this project is not hosted in this repository.*

Replace the line above with a real link or contact if you want users to report bugs somewhere public.
